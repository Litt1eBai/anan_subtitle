# PYSIDE6 LGPL NOTICE

本项目的 Windows 打包版本使用了 `PySide6` / Qt 运行库。

## 1. 许可证说明

`PySide6` 采用多重许可证发布，其中包含 `LGPL` 选项。
当前项目在二进制分发时按 `LGPL` 路径处理对应义务。

## 2. 当前分发方式

- 本项目使用 `PyInstaller` 目录分发形式
- `PySide6` / Qt 运行库以动态链接方式随发布目录分发
- 本项目自有源码许可证仍为 `MIT`

## 3. 发布包中应同时提供

- 项目 `LICENSE`
- `THIRD_PARTY_NOTICES.md`
- 本说明文件
- `PySide6` / Qt 对应的 `LGPL` 许可证文本

## 4. 说明

本文件是当前项目的分发提示文件，不替代 `PySide6` / Qt 官方许可证文本本身。正式对外发布前，应确保对应的官方 `LGPL` 文本也已随包附带。
